<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Termo de Cessão, Guarda, Uso e Devolução de Equipamento</title>
    <style>
        @media print {
            @page {
                size: A4 landscape;
                margin: 10mm;
            }
            body {
                background-color: #ffffff;
                transform: rotate(0deg); /* Garante que não haja rotação indesejada */
            }
            .email-button, #loadingScreen, #successMessage, .user-info-header {
                display: none;
            }
            .container {
                box-shadow: none;
                border: none;
                padding: 0;
                margin: 0;
                width: 100%;
            }
            table {
                page-break-inside: auto;
            }
            tr {
                page-break-inside: avoid;
                page-break-after: auto;
            }
        }

        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            color: #333;
            font-size: 10pt;
        }

        .container {
            width: 297mm; /* Altura A4 em paisagem */
            height: 210mm; /* Largura A4 em paisagem */
            margin: 10px auto;
            background: white;
            padding: 10mm;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            box-sizing: border-box;
            position: relative;
        }

        .header-image {
            width: 100%;
            text-align: center;
            margin-bottom: 10px;
        }

        .header-image img {
            max-width: 100%;
            height: auto;
            max-height: 40px;
        }

        .section-title {
            background-color: #E6E7E8;
            font-weight: bold;
            text-align: center;
            padding: 3px;
            font-size: 9pt;
            border: 1px solid #B3B3B3;
            margin-top: 10px;
            margin-bottom: 0;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 8px;
            font-size: 8pt;
            border: 1px solid #B3B3B3;
        }

        th, td {
            border: 1px solid #B3B3B3;
            padding: 3px 5px;
            text-align: left;
            vertical-align: top;
            height: 18px;
        }

        th {
            background-color: #E6E7E8;
            font-weight: bold;
            text-align: center;
        }

        .identification-table td {
            height: 20px;
        }

        .label {
            font-weight: bold;
            padding-right: 5px;
        }

        .rules-section {
            font-size: 8pt;
            margin-top: 5px;
            padding-left: 15px;
            text-align: justify;
        }

        .rules-section ul {
            list-style-type: none;
            padding-left: 0;
            margin-top: 5px;
        }

        .rules-section li {
            margin-bottom: 4px;
            position: relative;
            padding-left: 15px;
        }

        .rules-section li::before {
            content: "–";
            position: absolute;
            left: 0;
            top: 0;
        }

        .signature-line {
            border-bottom: 1px solid #333;
            height: 20px;
            margin-top: 10px;
            margin-bottom: 5px;
            position: relative;
        }

        .signature-label {
            font-size: 7pt;
            text-align: center;
        }

        .checkbox-label {
            margin-left: 3px;
            margin-right: 10px;
            font-size: 8pt;
        }

        .checkbox {
            width: 8px;
            height: 8px;
            border: 1px solid #333;
            display: inline-block;
            margin-right: 2px;
            vertical-align: middle;
        }

        .internal-info {
            text-align: center;
            font-weight: bold;
            padding-top: 10px;
            font-size: 8pt;
            border-top: 1px solid #B3B3B3;
            margin-top: 10px;
        }

        .internal-info span {
            display: block;
            margin-top: 2px;
            font-weight: normal;
        }

        .footer-info {
            display: flex;
            justify-content: space-between;
            font-size: 7pt;
            margin-top: 5px;
            color: #555;
        }

        /* Ajustes para paisagem */
        .two-columns {
            display: flex;
            justify-content: space-between;
            margin-top: 10px;
        }
        
        .column {
            width: 48%;
        }

        /* Elementos não visuais / funcionais */
        .user-info-header {
            display: none;
        }

        .email-button {
            position: fixed;
            bottom: 20px;
            right: 20px;
            background-color: #007bff;
            color: white;
            border: none;
            padding: 10px 15px;
            border-radius: 4px;
            cursor: pointer;
        }
    </style>
</head>
<body>

    <div class="container" id="content">
        <div class="header-image">
            <img src="/img/gpk.png" alt="GPK Logo">
        </div>

        <!-- Dados de Identificação -->
        <div class="section-title">DADOS DE IDENTIFICAÇÃO</div>
        <table class="identification-table">
            <tr>
                <td style="width: 40%;"><span class="label">Nome:</span> <%= nome %></td>
                <td style="width: 30%;"><span class="label">Unidade de Negócio:</span> <%= unidade %></td>
                <td style="width: 30%;"><span class="label">Razão Social (se PJ):</span> <% if (tipoUsuario === 'Prestador de Serviços') { %><%= cnpj %><% } else { %> N/A <% } %></td>
            </tr>
            <tr>
                <td><span class="label">CPF:</span> <% if (tipoUsuario === 'Colaborador') { %><%= cpf %><% } else { %> N/A <% } %></td>
                <td><span class="label">Área/Diretoria:</span> <%= setor %></td>
                <td><span class="label">Telefone:</span></td>
            </tr>
            <tr>
                <td colspan="3"><span class="label">E-mail:</span></td>
            </tr>
        </table>

        <!-- Identificação do(s) Equipamento(s) -->
        <div class="section-title">IDENTIFICAÇÃO DO(S) EQUIPAMENTO(S)</div>
        <table class="equipment-table">
            <thead>
                <tr>
                    <th style="width: 5%;">Item</th>
                    <th style="width: 25%;">Descritivo</th>
                    <th style="width: 15%;">Marca</th>
                    <th style="width: 15%;">Modelo</th>
                    <th style="width: 15%;">Serial</th>
                    <th style="width: 10%;">Uso<br><span style="font-size: 6pt; font-weight: normal;">(P = Permanente / T = Temporário)</span></th>
                    <th style="width: 10%;">Visto usuário</th>
                </tr>
            </thead>
            <tbody>
                <% if (typeof parsedUserData !== 'undefined' && parsedUserData.equipamentos && parsedUserData.equipamentos.length > 0) { %>
                    <% parsedUserData.equipamentos.forEach((equip, index) => { %>
                        <tr>
                            <td style="text-align: center;"><%= index + 1 %></td>
                            <td><%= equip.descritivo || 'N/A' %></td>
                            <td><%= equip.fabricante || 'N/A' %></td>
                            <td><%= equip.modelo || 'N/A' %></td>
                            <td><%= equip.numeroSerie || 'N/A' %></td>
                            <td></td>
                            <td></td>
                        </tr>
                    <% }); %>
                    <% for(let i = parsedUserData.equipamentos.length; i < 5; i++) { %>
                        <tr>
                            <td style="text-align: center;"><%= i + 1 %></td>
                            <td><% if (i===0) { %> Nenhum equipamento encontrado. <% } %></td>
                            <td></td>
                            <td></td>
                            <td></td>
                            <td></td>
                            <td></td>
                        </tr>
                    <% } %>
                <% } else { %>
                    <% for(let i = 0; i < 5; i++) { %>
                        <tr>
                            <td style="text-align: center;"><%= i + 1 %></td>
                            <td><% if (i===0) { %> Nenhum equipamento encontrado. <% } %></td>
                            <td></td>
                            <td></td>
                            <td></td>
                            <td></td>
                            <td></td>
                        </tr>
                    <% } %>
                <% } %>
            </tbody>
        </table>

        <!-- Divisão em duas colunas para paisagem -->
        <div class="two-columns">
            <!-- Coluna 1: Regras e Responsabilidades -->
            <div class="column">
                <div class="section-title">REGRAS, RESPONSABILIDADES E DECLARAÇÃO DE CESSÃO DE USO</div>
                <div style="border: 1px solid #B3B3B3; border-top: none; padding: 5px; font-size: 8pt; text-align: justify; height: 100%;">
                    Eu, <strong><%= nome %></strong>, declaro que recebi nesta data o(s) equipamento(s) acima citado(s) em perfeitas condições de uso, estou ciente de minhas responsabilidades e declaro ter conhecimento que:
                    <div class="rules-section">
                        <ul>
                            <li>A utilização do(s) equipamento(s) recebidos é exclusiva para exercício das atividades profissionais e que devo zelar pela guarda e conservação deles;</li>
                            <li>Caso o equipamento necessite de reparos, devo abrir chamado para o help desk, via ITCS – Portal TI;</li>
                            <li>Quando for necessária a substituição do equipamento (temporário ou efetivo), a identificação do novo equipamento deve ser registrada neste Termo e novamente coletada as assinaturas;</li>
                            <li>Em caso de desligamento o equipamento deve ser devolvido à área de TI, que registra a devolução no próprio termo e colhe a assinatura do usuário. Em caso de transferência entre Unidades de Negócio, o usuário deve devolver o equipamento e solicitar novo equipamento para TI a ser registrado no novo local de trabalho;</li>
                            <li>Em caso de perda ou dano irreparável, é responsabilidade do usuário restituir o valor correspondente ao equipamento, levando em consideração o tempo e a depreciação dele.</li>
                        </ul>
                    </div>
                    <div style="text-align: center; margin-top: 5px; font-size: 7pt;">
                        Clique ou toque aqui para inserir o texto., Clique ou toque aqui para inserir uma data..
                    </div>
                    <div style="display: flex; justify-content: space-between; margin-top: 15px;">
                        <div style="width: 48%;">
                            <div class="signature-line"></div>
                            <div class="signature-label">Responsável pela Entrega (nome/assinatura):</div>
                        </div>
                        <div style="width: 48%;">
                            <div class="signature-line"></div>
                            <div class="signature-label">Assinatura Usuário/Responsável Equipamento(s)</div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Coluna 2: Declaração de Devolução -->
            <div class="column">
                <div class="section-title">DECLARAÇÃO DE DEVOLUÇÃO</div>
                <div style="border: 1px solid #B3B3B3; border-top: none; padding: 5px; font-size: 8pt; height: 100%;">
                    Eu, __________________________________________________________________________, declaro que o(s) equipamento(s) sob a minha guarda foi(foram) devolvido(s) nas seguintes condições:
                    <table class="declaration-table" style="margin-top: 5px;">
                        <thead>
                            <tr>
                                <th style="width: 8%;">Item</th>
                                <th style="width: 50%;">Situação</th>
                                <th style="width: 22%;">Descrição do Estado/Situação do Equipamento</th>
                                <th style="width: 10%;">Data</th>
                                <th style="width: 10%;">Visto usuário</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% for(let i = 0; i < 5; i++) { %>
                                <tr>
                                    <td style="text-align: center;"><%= i + 1 %></td>
                                    <td>
                                        <span class="checkbox"></span><span class="checkbox-label">Em perfeito estado</span><br>
                                        <span class="checkbox"></span><span class="checkbox-label">Apresentando defeito</span><br>
                                        <span class="checkbox"></span><span class="checkbox-label">Faltando peças ou acessórios</span>
                                    </td>
                                    <td></td>
                                    <td></td>
                                    <td></td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <!-- Informações Internas e Rodapé -->
        <div class="internal-info">
            INTERNO
            <span>As informações contidas neste documento são destinadas ao uso interno.</span>
        </div>
        <div class="footer-info">
            <span>Página 1/1</span>
            <span>FR.14.01.001<br>Versão 05 – 11/08/2023</span>
        </div>
    </div>

    <button class="email-button" onclick="sendEmail()">
        Enviar Termo por E-mail
    </button>

    <script>
        function sendEmail() {
            // Simulação do envio de e-mail
            alert("Funcionalidade de envio por e-mail seria implementada aqui");
        }
    </script>
</body>
</html>